export default function EditorActions({ editor, saving, onSave, saveLabel = 'Kataloğa Kaydet', namePlaceholder = 'Çerçeve adı' }) {
  const download = async () => { const file = await editor.makeFile(), url = URL.createObjectURL(file), link = document.createElement('a'); link.href = url; link.download = file.name; link.click(); URL.revokeObjectURL(url); };
  return <div className="space-y-2 border-t border-border bg-card p-3"><div className="grid grid-cols-3 gap-2"><button onClick={() => editor.setShowMask(!editor.showMask)} className="min-h-11 rounded-xl bg-secondary text-sm font-bold">👁 {editor.showMask ? 'Gizle' : 'Göster'}</button><button onClick={() => editor.setMaskView(!editor.maskView)} className="min-h-11 rounded-xl bg-secondary text-sm font-bold">Maske</button><button onClick={editor.resetSelection} className="min-h-11 rounded-xl bg-secondary text-sm font-bold">Seçimi Sil</button></div><button onClick={editor.apply} className="min-h-14 w-full rounded-xl bg-primary text-lg font-extrabold text-primary-foreground">✨ ŞEFFAFLAŞTIR</button>{editor.applied && <><input value={editor.name} onChange={(e) => editor.setName(e.target.value)} placeholder={namePlaceholder} className="min-h-12 w-full rounded-xl border border-border bg-secondary px-3 text-base" /><div className="grid grid-cols-2 gap-2"><button onClick={download} className="min-h-12 rounded-xl bg-secondary font-bold">PNG İndir</button><button onClick={onSave} disabled={saving} className="min-h-12 rounded-xl bg-primary font-bold text-primary-foreground disabled:opacity-50">{saving ? 'Kaydediliyor' : saveLabel}</button></div></>}</div>;
}